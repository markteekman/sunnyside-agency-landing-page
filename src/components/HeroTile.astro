<section class="hero-tile">
  <h1>We are creatives</h1>
  <a href="#next-section" aria-label="Go to next section">
    <svg width="36" height="114" viewBox="0 0 36 114" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M3 95.484L18 110.484L33 95.484M18 3V103V3Z" stroke="#212E3C" stroke-width="6" stroke-linecap="round" stroke-linejoin="round"/></svg>
  </a>
</section>

<style lang="scss">
  @use "../styles/base/breakpoint" as *;
  @use "../styles/base/size" as *;

  .hero-tile {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 3rem;
    color: var(--neutral-clr-dark-blue);
    background-image: url('/assets/mobile/image-header.jpg');
    background-size: cover;
    background-position: center center;
    background-repeat: no-repeat;
    width: 100%;
    height: 80vh;

    h1 {
      text-align: center;
      font-size: 2.5rem;
      line-height: 3.25rem;
      text-transform: uppercase;
      letter-spacing: 0.5rem;
      padding-top: 4rem;
      animation: fade-in 0.3s ease-out;

      @include breakpoint(medium) {
        font-size: 3.5rem;
        padding-top: 8rem;
        padding-bottom: 5rem;
      }
    }

    svg {
      animation: fade-down 3s infinite alternate ease-in-out;
    }

    @include breakpoint(medium) {
      background-image: url('/assets/image-header.jpg');
      background-position: center;
    }
  }

  @keyframes fade-in {
    from {
      opacity: 0;
      transform: translateY(5rem);
    }

    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes fade-down {
    0% {
      opacity: 0;
      transform: translateY(-2rem);
    }

    15% {
      opacity: 1;
      transform: translateY(0);
    }
  }
</style>